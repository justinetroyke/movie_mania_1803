<h1><%= @genre.name %></h1>

<h2><%= "Average Rating: #{@genre.average_rating}" %></h2>

<body>
  <% @movies.each do |movie| %>
    <h3><%= link_to movie.title, movie_path(movie) %></h3>
  <% end %>
</body>
